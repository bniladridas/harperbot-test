# Usage (test)

This is a test file to validate HarperBot:

- multi-file diffs
- markdown parsing
- inline suggestions and `/apply`

## Commands

Comment these in the PR:

- `/analyze`
- `/apply`
- `/pause` then push a commit
- `/resume` then push a commit

